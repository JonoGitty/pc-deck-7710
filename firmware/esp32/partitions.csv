# Partition table — 16 MB flash (ESP32-WROVER-E is sold in 4/8/16 MB).
#
# The size of this file is a direct consequence of one number: a 256x64 baked
# movie is most of a megabyte. SOLAR is 848 KB, TOUGE 282 KB, DOLPHINS 347 KB.
# They cannot go in the app image — an app slot is 1.5 MB and that has to hold
# Bluetooth, WiFi, TLS, the FFT and the renderer — so they get their own
# read-only partition and the decoder streams out of it. See core/movie.h.
#
# Three app slots, not two. Updates arrive over BLE from a phone with no cable
# and no console, so `factory` is the image that always boots and is never
# written again; ota_0 and ota_1 take turns receiving updates and roll back to
# each other. A car with no stereo until the dash comes apart is the failure
# this is paying 1.5 MB to avoid. See docs/VERSIONING.md.
#
# On a 4 MB part none of this fits. Either drop to one app slot and give up
# rollback, or put movies on an SD card — the streaming source does not care
# which, and that is the point of it.
#
# Offsets are exact: the table ends on 0x1000000 with no gap and no overlap.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
factory,    app,  factory,  0x10000,  0x180000,
ota_0,      app,  ota_0,    0x190000, 0x180000,
ota_1,      app,  ota_1,    0x310000, 0x180000,
# Everything below is content, not code, and survives an OTA untouched.
movies,     data, 0x40,     0x490000, 0x740000,
storage,    data, spiffs,   0xBD0000, 0x430000,
