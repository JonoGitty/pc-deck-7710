# scratch / os
# node_modules was once committed here as a SYMLINK into a build machine's
# temp directory — broken on every clone, and a scratch path leaked into a
# public repository. Playwright is a dev dependency of the verify suite and
# nothing that runs the deck needs it.
node_modules/
node_modules
Thumbs.db
desktop.ini
*.log
build/
legacy/web/media/
__pycache__/
*.pyc
.venv/
preview/deck.wasm
movies/*.json

# ESP-IDF build output
firmware/esp32/build/
firmware/esp32/build-*/
firmware/esp32/sdkconfig
firmware/esp32/sdkconfig.old
firmware/esp32/managed_components/
firmware/esp32/dependencies.lock
build/sim
build/sim.raw
